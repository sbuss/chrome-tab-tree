{
  "name": "chrome-tab-tree",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:visual": "playwright test test/visual/visual.test.js",
    "test:visual:update": "playwright test test/visual/visual.test.js --update-snapshots"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "serve": "^14.2.6",
    "vitest": "^3.1.0"
  }
}
